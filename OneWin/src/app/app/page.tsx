import { AppClose } from "@/components/AppClose";

export const metadata = {
  title: "Today’s close — One Win",
  description: "Log today’s one win and tomorrow’s one move.",
};

export default function AppPage() {
  return <AppClose />;
}
