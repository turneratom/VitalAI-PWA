import { QuizFlow } from "@/components/QuizFlow";

export const metadata = {
  title: "Quiz — One Win",
  description: "Ten questions about chaos, role, and what fails by 9pm.",
};

export default function QuizPage() {
  return <QuizFlow />;
}
