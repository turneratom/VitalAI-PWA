import { PaywallView } from "@/components/PaywallView";

export const metadata = {
  title: "Keep the close — One Win",
  description: "One Win membership. $6.99 per month or $49 per year.",
};

export default function PaywallPage() {
  return <PaywallView />;
}
