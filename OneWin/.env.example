# One Win env (optional)
# If unset, checkout buttons continue to /thanks so the local demo still works.
NEXT_PUBLIC_CHECKOUT_MONTHLY=/thanks
NEXT_PUBLIC_CHECKOUT_YEARLY=/thanks

# Used for metadata / Open Graph absolute URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
