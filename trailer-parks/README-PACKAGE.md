# App package manifests live at the repo root so Vercel Git deploys
# (rootDirectory=null) can detect Next.js. Source remains under ./src
# and ./public (symlinks into this folder).
#
# From repo root:
#   npm install
#   npm run dev
#
# The previous package.json is preserved as package.app.json for reference.
