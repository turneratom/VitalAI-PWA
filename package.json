{
  "name": "vitalai-workspace",
  "private": true,
  "description": "Monorepo root. Next app lives in trailer-parks/; Vercel builds via @vercel/next on trailer-parks/package.json.",
  "scripts": {
    "build": "npm run build --prefix trailer-parks",
    "dev": "npm run dev --prefix trailer-parks",
    "lint": "npm run lint --prefix trailer-parks"
  }
}
